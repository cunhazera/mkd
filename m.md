This line ends with two trailing spaces  
This line ends with a backslash\
This line is inside a block where a blank line creates a paragraph break

This line started after a blank line above
This line is inside an HTML break tag<br>and this continues after it
This line uses an explicit HTML line break<br/>and continues right after
This line is inside a `<pre>` block so newlines render literally
This line ends with four trailing spaces    
This line uses a zero-width space followed by newline (​
)
